import React, { useState, useEffect, useMemo, useRef } from 'react';
import 'animate.css';

const Accounts = () => {

  useEffect(() => {

  }, []);


  return (
    <div className={`${styles.accountsContainer}`}></div>
  );
};

export default Accounts;
